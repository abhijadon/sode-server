const {
  create,
  pagination,
  read,
  remove,
  update,
  selectOptions,
  selectWebsiteOptions,
} = require("./crud.controller");

module.exports = {
  create,
  pagination,
  read,
  remove,
  update,
  selectOptions,
  selectWebsiteOptions,
};
