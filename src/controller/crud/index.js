const {
  create,
  pagination,
  read,
  remove,
  update,
  selectOptions,
} = require("./crud.controller");

module.exports = {
  create,
  pagination,
  read,
  remove,
  update,
  selectOptions,
};
